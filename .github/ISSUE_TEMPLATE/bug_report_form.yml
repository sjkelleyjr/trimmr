name: Bug report (form)
description: Same bug checklist as the default template, as structured GitHub fields.
title: "bug: "
labels:
  - bug
body:
  - type: textarea
    id: intro
    attributes:
      label: Context
      description: Shown in the issue; edit or delete if you prefer.
      value: |
        We try to limit the analytics we track in trimmr so we rely heavily on community bug reports.  Thank you for taking the time to report this bug! ❤️

        Please fill in all required fields so we can reproduce and debug quickly.
    validations:
      required: false
  - type: textarea
    id: summary
    attributes:
      label: What happened?
      description: Describe the bug and what you expected instead.
      placeholder: "When I ..., I expected ..., but ..."
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      description: Use numbered steps and include the exact source file when possible.
      placeholder: |
        1. Open ...
        2. Import ...
        3. Click ...
    validations:
      required: true
  - type: dropdown
    id: source-kind
    attributes:
      label: Source kind
      options:
        - video
        - animated-image
        - unknown
    validations:
      required: true
  - type: input
    id: source-format
    attributes:
      label: Source format
      description: Example: gif, webm, mp4, apng, animated-webp
      placeholder: gif
    validations:
      required: true
  - type: input
    id: source-file
    attributes:
      label: Source filename
      description: Include extension
      placeholder: sample.gif
    validations:
      required: true
  - type: input
    id: source-duration
    attributes:
      label: Reported source duration (ms)
      placeholder: "6600"
    validations:
      required: true
  - type: input
    id: clip-settings
    attributes:
      label: Clip settings
      description: trim start/end + playback rate
      placeholder: trim 0-6600ms, rate 1.0x
    validations:
      required: true
  - type: input
    id: browser
    attributes:
      label: Browser and version
      placeholder: Safari 18.0 (macOS 15.0)
    validations:
      required: true
  - type: textarea
    id: user-agent
    attributes:
      label: Browser user agent
      render: shell
      placeholder: Paste `navigator.userAgent`
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Console logs
      description: Paste any `[trimmr:*]` logs from DevTools.
      render: shell
    validations:
      required: false
  - type: textarea
    id: attachments
    attributes:
      label: Screenshots / sample media
      description: Drag-and-drop screenshots, recordings, and a minimal sample file if possible.
    validations:
      required: false
